# Repository Context

This project uses two repositories:

* Main repository: `nu-shinkan`
* Wiki repository: `nu-shinkan.wiki`

The wiki repository must be available at:

`[nu-shinkan repository root]/nu-shinkan.wiki/`

If `nu-shinkan.wiki/` is missing or unavailable at that location, stop the task and report the problem. Do not proceed by assuming or reconstructing its contents.

Before performing substantial repository work, follow:

`nu-shinkan.wiki/instructions/context-discovery.md`

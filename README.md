# nu-shinkan

このリポジトリは，nu-shinkanプロジェクトのコードベースとして機能することを意図したモノレポであり，ビルド/モノレポ上のコマンド操作補助ツールとして，turborepoを利用しています．

## 目次

- [nu-shinkan](#nu-shinkan)
  - [目次](#目次)
  - [開発を始める前に](#開発を始める前に)
  - [開発環境](#開発環境)
  - [各種規則について](#各種規則について)
  - [ディレクトリ構成](#ディレクトリ構成)
    - [ディレクトリ](#ディレクトリ)
    - [ファイル](#ファイル)
  - [ローカルワークスペース](#ローカルワークスペース)
  - [実行時環境変数](#実行時環境変数)
    - [実行時環境変数とは](#実行時環境変数とは)
    - [フロントエンドパッケージ](#フロントエンドパッケージ)
    - [バックエンドパッケージ](#バックエンドパッケージ)
    - [複数のパッケージで共有される環境変数](#複数のパッケージで共有される環境変数)
  - [コマンド](#コマンド)
    - [`make:frontend`：フロントエンドパッケージの生成](#makefrontendフロントエンドパッケージの生成)
    - [`make:backend`：バックエンドパッケージの生成](#makebackendバックエンドパッケージの生成)
    - [`sync`：共有の設定値の同期など](#sync共有の設定値の同期など)
    - [`dev`: ローカルサーバの立ち上げ](#dev-ローカルサーバの立ち上げ)
  - [デプロイ](#デプロイ)

## 開発を始める前に

本ワークスペースは，`devcontainer`で開いてください．

`docs`リポジトリが自動でクローンされるはずですが，クローンされなかった場合は
リポジトリルートで次を実行してください．

```sh
git clone https://github.com/nu-shinkan-project/docs.git docs
```

## 開発環境

このリポジトリでは，以下のツールを採用しています．

- パッケージマネージャ: **pnpm**
- ビルドツール: **turborepo**

また，近年パッケージマネージャを介してクレデンシャルが剽窃されるインシデントが多発しているため，基本的には**devcontainer**を利用して隔離環境下で開発をすすめます．

## 各種規則について

文書運用規則，Git運用規則（ブランチ戦略など），レビュー規約などは，[docsリポジトリ](https://github.com/nu-shinkan-project/docs)の[`policy/`](https://github.com/nu-shinkan-project/docs/tree/main/policy)に保管されています．

## ディレクトリ構成

リポジトリルートに存在する主なアイテムについて説明します．

### ディレクトリ

- **`apps/`**
  デプロイされる単位（マイクロサービス，アプリケーション）を配置します．
  たとえば，_一般ユーザ向けWebアプリ_, _組織ユーザ管理サービス_ などが該当します．
  **開発者が主に触る場所です．**
  それぞれの詳細は`apps/*/README.md`を参照してください．
- **`packages/`**
  複数のアプリケーションから再利用されるコードが配置されます．（ユーザ定義パッケージという位置づけです．）
- **`scripts/`:**
  このディレクトリの配下には，ローカル（開発環境）で実行されるスクリプトが配置されます．
  たとえば，テスト用のデータの自動投入スクリプトなどはここに配置されます．
  `packages/`配下にも`package-script/`がありますが，`package-script/`には文脈がパッケージ単位に閉じたスクリプト，`scripts/`には文脈がリポジトリにまたがるスクリプトを配置する形で使い分けます．
- **`docs/`:**
  グローバル文書を役割別に配置する別の Git リポジトリです．
  文書配置については[文書運用規則](https://github.com/nu-shinkan-project/docs/blob/main/policy/documentation.md)を確認してください．
  メインリポジトリでは Git 管理対象外です．
  AIエージェントへの指示の整合性のため，リポジトリルートにクローンしてください．
- **`templates/`:**
  このディレクトリの配下には，他のディレクトリ（典型的には，`app/*`）の雛形となるディレクトリが配置されています．

### ファイル

- **`AGENTS.md`:**
  AIエージェント向けの文書です．AIエージェントはまず最初にこれを読むことが期待されています．
- **`README.md`:**
  このリポジトリを説明する文書です．人間はまず最初にこれを読むことが期待されています．プロダクトの要約や，見るべきドキュメントの案内などを含みます．
- **`package.json`:**
  _リポジトリルートに存在する_`package.json`は，開発環境・CI/CD環境を構築するために必要な依存関係などを定めます．（appごとに必要な依存関係は，`apps/*/package.json`で定めます．）
- **`pnpm-workspace.yaml`**
  ワークスペースの構造を規定したり，pre/build/post install scriptの実行を許可したりするための設定ファイルです．

## ローカルワークスペース

ローカルワークスペースとは，git管理されない作業スペースです．
典型的に，AIエージェントと，ファイルを介してやり取りをする場として，`.agents/handoff`が想定されています．

実装計画書や，調査報告書など，gitのライフサイクルで管理することが適当でない文書はここに配置してください．

## 実行時環境変数

実行時環境変数の概念，および設定方法を説明します．

### 実行時環境変数とは

実行時環境変数は，

- フロントエンドパッケージでは，ユーザのブラウザ（クライアントサイド）
- バックエンドパッケージでは，クラウドサーバー（cloudflare）

で参照できる環境変数 (process.envでアクセスできる変数) です．

自分のPCでのみ参照できる通常の環境変数とは異なります．

たとえば，通常の環境変数Aが自分のPCにあっても，クライアントやクラウドはそれにアクセスする術を持ちません．
そのため，クライアントサイドで動くフロントエンドのコード，クラウドサーバ上で動くバックエンドのコードから直接環境変数Aを取得しても，未定義となります．

```plain
         +-local-----------------+
env var: | A=some val            |
         +-----------------------+
code     | print(A); // some val |
         +-----------------------+

         +-client-----------------+
env var: |                        |
         +------------------------+
code     | print(A); // undefined |
         +------------------------+

         +-cloud------------------+
env var: |                        |
         +------------------------+
code     | print(A); // undefined |
         +------------------------+
```

一方で，ビルド時に組み込んだり，デプロイ時に明示的に受け渡す実行時環境変数Bは，それぞれの環境から参照することができます．

```plain
         +-local-----------------+
env var: | B=some val            |
         +-----------------------+
code     | print(B); // some val |
         +-----------------------+

         +-client-------------------------+          +-local------+
env var: |                                |          | B=some val |
         +--------------------------------+          +------------+
code     | print("some val"); // some val | <-build- | print(B);  |
         +--------------------------------+          +------------+

         +-cloud------------------+            +-local------+
env var: | B=some val             | <-specify- | B=some val |
         +------------------------+            +------------+
code     | print(B); // some val  |            | print(B);  |
         +------------------------+            +------------+
```

### フロントエンドパッケージ

ここから，フロントエンドパッケージにおいて実行時環境変数（ビルド時に組み込まれる変数）を使う方法を説明します．
フロントエンドではビルドツールとしてviteを用いているため，viteの流儀に従います．

viteでは，プレフィックスに`VITE_`とついた環境変数は，ビルド時に組み込むことになっています．
なので，環境変数の命名を`VITE_XXXX`とします．

また，`.env.development`の定義を，ローカルサーバ (`pnpm dev`で立ち上がる開発用サーバ) が環境変数として参照できるように設定してあります．
さらに，デプロイ時にも`.env.development`の内容を引き継ぐようにしてあります．（明示的に除外された時を除く）

また，デプロイ先によって環境変数の定義を変更したい場合は，`development.yaml`に記述します．
`deployment.yaml`の詳しい書き方は[configuration-design](docs/design/configuration-design.md)を参照してください．

### バックエンドパッケージ

ここから，バックエンドパッケージにおいて実行時環境変数（クラウドにアップロードされる変数）を指定する方法を説明します．

cloudflareでは，`wrangler.jsonc`というファイルでデプロイの設定を行います．`wrangler.jsonc`は，コメントができるJSON形式のファイルです．
`wrangler.jsonc`の"vars"フィールドの内容が，クラウドサーバにおいて環境変数として設定されます．

### 複数のパッケージで共有される環境変数

ここから，複数のパッケージで共有される環境変数の設定方法を説明します．
素朴な方法は，その環境変数を利用する各パッケージで個別に同じ値で定義することです．
しかし，この方法では，修正時に触るべき箇所が多く，変更漏れを生じる危険性があります．

そこで，このリポジトリでは，複数パッケージ間で共有される変数を記述する場所として，`packages/app-config/globalRuntimeEnvs.yaml`を用意しています．`packages/app-config/globalRuntimeEnvs.yaml`の詳しい書き方は[configuration-design](docs/design/configuration-design.md)を参照してください．

`packages/app-config/globalRuntimeEnvs.yaml`に記述した内容は，リポジトリルートの`pnpm sync`スクリプトによって利用する全パッケージに反映できます．

## コマンド

開発を便利に進めるために，いくつかのスクリプトやコマンドが用意されています．
ここで示すコマンドは，リポジトリルートで実行してください．

### `make:frontend`：フロントエンドパッケージの生成

```sh
pnpm make:frontend
```

対話的にアプリ名を入力すると，`templates/frontend-template/`をもとに`apps/<アプリ名>/`へフロントエンドパッケージを生成します．開発用ポートを自動で割り当て，依存関係をインストールします．

### `make:backend`：バックエンドパッケージの生成

```sh
pnpm make:backend
```

対話的にアプリ名を入力すると，`templates/backend-template/`をもとに`apps/<アプリ名>/`へバックエンドパッケージを生成します．開発用・Inspector用ポートを自動で割り当て，依存関係のインストールとCloudflareの型生成を実行します．

### `sync`：共有の設定値の同期など

```sh
pnpm sync
```

`packages/app-config/globalRuntimeEnvs.yaml`や各パッケージの`deployment.yaml`に基づいて，
設定ファイル（`.env.development`, `wrangler.jsonc`）の内容を最新化します．

`packages/app-config/globalRuntimeEnvs.yaml`や`deployment.yaml`の詳しい書き方は[configuration-design](docs/design/configuration-design.md)を参照してください．

### `dev`: ローカルサーバの立ち上げ

```sh
pnpm dev
```

すべてのローカルサーバ（動作検証用サーバ）をまとめて立ち上げます．
このときのTUI画面は，マウス操作も可能です．

## デプロイ

クレデンシャルの窃盗リスクを抑えるため，デプロイはローカルでは行わず，github runnners (actions)上で行うことを基本とします．
CI/CDの設定により，開発者は

- GithubへのPush
- タグの付け替え

によってデプロイをトリガーすることが可能です．
